version: v1alpha1

web:
    build:
        type: Dockerfile    
        dockerfile:
            contents: |
                        FROM alpine11111 as build
                        WORKDIR /app
                        COPY . .
    output:
        type: static
        static:
            path: /app
